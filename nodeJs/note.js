/* const path = require('path');
const fs = require('fs')
 */
//Base name
/* console.log(path.basename(__filename)) */
//Dirname
/* console.log(path.__dirname(__filename)) */
//Create file
/* fs.mkdir(path.join(__dirname,'/test'),{},err=>{
    if (err) throw err
    console.log('File created..');
}) */
//Write a flie
/* fs.writeFile(path.join(__dirname,'/test','hello.txt'),__filename,err=>{
    if (err) throw err
    console.log('File edited..');
}) */
/* fs.appendFile(path.join(__dirname,'/test','hello.txt'),'LOve NodeJs',err=>{
    if (err) throw err
    console.log('File edited..');
}) */
//read a file
/* fs.readFile(path.join(__dirname,'/test/hello.txt'),'utf8',(err,data)=>{
    if (err) throw err
    console.log(`File add.. ${data}`);
}) */
